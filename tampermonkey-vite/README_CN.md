# Cookie Share - Vite + Vue 3 + TypeScript

[English](./README.md) | [简体中文](./README_CN.md)

使用现代技术栈重写的 Cookie Share 油猴脚本，具有以下特性：
- **vite-plugin-monkey** - 用于油猴脚本开发的 Vite 插件
- **Vue 3** - 使用 Composition API 的渐进式 JavaScript 框架
- **TypeScript** - 类型安全的 JavaScript
- **UnoCSS** - 即时按需的原子 CSS 引擎（兼容 Tailwind CSS）
- **DaisyUI** - 简洁美观的 UI 组件库

## 项目结构

```
tampermonkey-vite/
├── src/
│   ├── components/        # Vue UI 组件
│   │   ├── FloatingButton.vue   # 页面悬浮按钮
│   │   ├── MainModal.vue        # 主控制面板
│   │   ├── ListModal.vue        # Cookie 列表弹窗
│   │   ├── SettingsPanel.vue    # 设置面板
│   │   └── Notification.vue     # 消息通知
│   ├── locales/           # i18n 国际化
│   │   └── index.ts       # 中英文翻译
│   ├── services/          # 业务逻辑服务
│   │   ├── api.ts         # 服务器 API 通信
│   │   ├── cookie.ts      # Cookie CRUD 操作
│   │   └── storage.ts     # GM 存储封装
│   ├── store/             # 状态管理
│   │   └── index.ts       # 响应式状态与操作
│   ├── styles/            # CSS 样式
│   │   └── daisyui.css    # DaisyUI 组件样式
│   ├── types/             # TypeScript 类型定义
│   │   └── cookie.ts      # Cookie 相关类型
│   ├── App.vue            # 根组件
│   ├── main.ts            # 入口文件
│   └── vite-env.d.ts      # TypeScript 声明文件
├── dist/
│   └── cookie-share.user.js   # 构建后的油猴脚本
├── uno.config.ts          # UnoCSS 配置
├── vite.config.ts         # Vite 配置
├── package.json
└── README.md
```

## 开发指南

```bash
# 安装依赖
pnpm install

# 启动开发服务器 (支持热重载)
pnpm dev

# 构建生产版本
pnpm build
```

## 功能特性

- 🍪 在不同浏览器之间发送和接收 Cookie
- 💾 支持本地存储或云端服务器同步
- 🌐 多语言支持 (简体中文 & English)
- 🎨 基于 DaisyUI 的现代美观界面
- ⌨️ 快捷键支持 (Alt+Shift+C, Alt+Shift+L)
- 🔒 云端操作支持管理员密码保护
- 📱 响应式设计

## 快捷键

- `Alt + Shift + C` - 开关 Cookie Share 面板
- `Alt + Shift + L` - 开关 Cookie 列表

## 项目架构

代码组织遵循关注点分离原则：

### 服务层 (Business Logic)
- `cookie.ts` - 封装 GM_cookie，处理 Cookie 操作
- `api.ts` - 与后端服务器通信，处理云端同步
- `storage.ts` - 封装 GM_getValue/setValue，处理插件设置

### 状态管理 (State Management)
- 使用 Vue 3 的 `reactive()` 和 `ref()` 实现响应式状态
- 定义 UI 状态、设置项和 Cookie 操作的 Actions
- 使用计算属性处理派生状态

### 组件层 (UI)
- 纯展示性组件
- 不直接调用服务，通过 Store Actions 进行交互
- 使用 DaisyUI 类名进行样式控制

### 国际化 (i18n)
- 类型安全的翻译键名
- 支持运行时语言切换
- 自动检测浏览器语言

## 许可证

MIT

